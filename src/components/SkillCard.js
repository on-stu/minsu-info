import React from "react";
import styled from "styled-components";
const Container = styled.div`
  width: 100%;
`;
const SkillCard = () => {
  return <Container>SkillCard</Container>;
};

export default SkillCard;
